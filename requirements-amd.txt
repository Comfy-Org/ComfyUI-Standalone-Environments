# AMD GPU requirements (ROCm 7.14, Linux)
# Versions are pinned to prevent future pytorch releases from silently changing built environments.
#
# ROCm 7.14 ships via AMD's TheRock multi-arch wheel index, which serves both
# Linux and Windows from the same URL. The [device-all] extra pulls in the
# matching ROCm device libraries for all supported GPU architectures, so no
# separate rocm-sdk pins are needed.

--index-url https://repo.amd.com/rocm/whl-multi-arch/
--extra-index-url https://pypi.org/simple

torch[device-all]==2.12.0+rocm7.14.0
torchvision[device-all]==0.27.0+rocm7.14.0
torchaudio==2.11.0+rocm7.14.0
